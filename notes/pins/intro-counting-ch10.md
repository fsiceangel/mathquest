# Pins — intro-counting ch10 (Geometric Probability)

Read `notes/variation-spec.md` first. Every KEY below was computed exactly in node from the
designed region (closed-form length/area over total) and cross-checked by Monte Carlo of
the exact stated condition (2M samples, all within 4σ). Do not retarget a pin; if your own
computation disagrees, STOP and report. Answer indices are derived from the base:
v1 = (b+1)%4, v2 = (b+3)%4, v3 = (b+2)%4. Worksheet items are free response (no indices);
their keys are LaTeX strings, e.g. `'$\\frac{7}{11}$'`.

Conventions to state in every stem: "chosen at random" means uniformly; intervals of real
numbers (never counting integers — endpoints weigh nothing, so < vs ≤ never changes a key);
two random numbers are always independent; answers keep exact form ($\pi$, $\sqrt{2}$,
fractions — never decimals). Every AREA slot keeps a to-scale figure drawn with the
`arcPts`/`circlePts`/`ring` helpers at the top of `ch10.js`; each area pin below names the
total region and the favorable sub-region — draw exactly those, and never label the
quantity asked. The hardening edit is in place: base challenge slot 10 is the
stick-cut-into-three-pieces triangle problem (key $\frac{1}{4}$); do not touch the base file.

## Already taken — do not reuse any of these number sets or scenarios

Base problems + learn examples in `ch10.js`:
- Lengths: segment $18$ within $3$ of $A$ ($\frac{1}{6}$); $[2,12]$ less than $5$ ($\frac{3}{10}$); bus every $15$ wait $<6$ ($\frac{2}{5}$); $A$–$C$–$B$ with $3,7$ closer to $C$ than $B$ ($\frac{13}{20}$); traffic light $40/50/5$ red-or-yellow ($\frac{9}{19}$); $[-4,6]$ $x^3<8$ ($\frac{3}{5}$); $24$-inch wire shorter piece $\ge 8$ ($\frac{1}{3}$); $[0,20]$ $|x-8|<3$ or $|x-14|<4$ ($\frac{13}{20}$); $PA \cdot PB > 16$ on length $10$ ($\frac{3}{5}$); routes every $12$ / every $20$ offset $5$, wait $<3$ ($\frac{11}{30}$); learn: segment $15$ within $3$ of an endpoint, tram every $12$ wait $>9$, $[-3,5]$ $x^2<4$.
- Areas: triangle base $8$ height $8$ in $12 \times 8$ ($\frac{1}{3}$); quarter circle $r6$ in square $6$ ($\frac{\pi}{4}$); square $8$ within $1$ of a side ($\frac{7}{16}$); $[0,4]^2$ $x+y<2$ ($\frac{1}{8}$); bullseye $r2$ in target $r6$ ($\frac{1}{9}$); square $6$ closer to side $AB$ than the other three ($\frac{1}{4}$); semicircle $r2$ diameter on left side of $6 \times 4$ ($\frac{\pi}{12}$); $[0,6]^2$ $|x-y| \ge 2$ ($\frac{4}{9}$); Ana waits $10$ / Ben waits $20$ on $[0,60]^2$ ($\frac{31}{72}$); $x, y, 3$ triangle on $[0,6]^2$ ($\frac{5}{8}$); learn: $4 \times 3$ painted in $10 \times 6$, circle $r2$ in square $8$, meet-within-$20$ on $[0,60]^2$, $|x-y| \le 15$ on $[0,60]^2$.
- Challenge: segment $25$ within $4$ of midpoint; $[3,13]$ $|x-6| \le 2$; square $4$ centered in circle $r4$ ($\frac{1}{\pi}$); crosswalk WALK $25$ / DON’T WALK $35$ start within $10$; disk $r6$ closer to edge than center ($\frac{3}{4}$); $P$ on $AB$ of square $6$, area $PBC < 6$; four corner quarter circles $r1$ in square $4$ ($\frac{\pi}{16}$); Cleo $[0,60]$ × Dev $[0,30]$ within $10$ ($\frac{11}{36}$); $|x-2|+|y-2|<1$ on $[0,4]^2$; stick $12$ two cuts triangle ($\frac{1}{4}$); clock hands smaller angle $<30°$ ($\frac{1}{6}$); each $<$ twice the other and sum $>6$ on $[0,6]^2$ ($\frac{1}{3}$).
- Worksheet: $A$–$B$–$C$ with $5,9$; ferry every $25$ wait $5$–$15$; $[0,20]$ $\sqrt{x}<3$; two circles $r2$ in square $8$; pool $12 \times 8$ with two $2$-m decks; $[0,5]^2$ $y>2x$; $30$-min window Amy first Ben $>10$ later; square inscribed in circle $r3$ ($\frac{2}{\pi}$); $[0,12]$ within $1$ of a multiple of $5$; $[0,6]^2$ $4<x+y<8$.

Shaded-region problems in `src/data/intro-geometry/ch11.js` (also off limits):
rings with radii $4$&$2$, $5$&$3$, $7$&$4$, outer circumference $20\pi$ & inner $6$, ring area $27\pi$ with outer $=2\times$inner; circle inscribed in square side $4$ or $6$ (inside square, outside circle); square inscribed in circle $r5$ or $r6$ (inside circle, outside square); circular segments at $90°$ ($r4$, $r6$, $r8$) and $60°$ ($r6$, $r12$); quarter circle $r10$; semicircle diameter $8$; lens overlap of two quarter circles $r4$ in square $4$; big circle $r6$ minus two circles $r3$; semicircle $r6$ vs two $r3$; square $10$ with four corner quarter circles $r5$ (middle region); goat on rope $6$ at corner of shed $8$; window square $8$ + semicircle.

Geometric-probability items in `src/data/intro-counting/ch15.js` + `notes/pins/intro-counting-ch15.md` (also off limits):
$6$-m ribbon two cuts all pieces $<4$ ($\frac{2}{3}$); $x \in [0,3], y \in [0,2]$ differ $<1$; $[0,4] \times [0,3]$ differ $<2$ ($\frac{19}{24}$); $[0,4] \times [0,2]$ $1<x+y<4$ ($\frac{11}{16}$); $[0,3] \times [0,2]$ given differ $<1$, $x>2$ ($\frac{1}{7}$); expected distance to nearest side in $3 \times 2$ and $4 \times 2$; right triangle legs $3,4$ distance to hypotenuse; $3 \times 2$ distance to nearest side $>\frac{1}{2}$ ($\frac{1}{3}$); $[0,4] \times [0,2]$ $x<y<2x$ ($\frac{1}{8}$); $[0,5] \times [0,2]$ $x<y<2x$ ($\frac{1}{10}$); $[0,2]^2$ $y<\frac{x}{2}$ or $y>2x$ ($\frac{1}{2}$); reversed $x<y<2x$ with $a=6$.

## §10.2 Probability Using Lengths — ch10-s1.js
base indices: 1 0 2 3 0 2 1 3 0 2
v1 indices:   2 1 3 0 1 3 2 0 1 3
v2 indices:   0 3 1 2 3 1 0 2 3 1
v3 indices:   3 2 0 1 2 0 3 1 2 0

| slot | v1 | v2 | v3 |
|---|---|---|---|
| 1 | A point is chosen at random on segment $CD$ of length $21$. Probability it is within $6$ units of $D$. **KEY: $\frac{2}{7}$** | A $45$-second song plays; a listener tunes in at a random moment during it. Probability the moment falls in the first $9$ seconds. **KEY: $\frac{1}{5}$** | *Reversed:* A point chosen at random on a segment of length $L$ is within $4$ units of endpoint $A$ with probability $\frac{1}{9}$. Find $L$. **KEY: $36$** |
| 2 | A real number is chosen at random from $[1, 17]$. Probability it is less than $7$. **KEY: $\frac{3}{8}$** | A gauge needle settles at a random reading in $[-6, 10]$. Probability the reading is positive. **KEY: $\frac{5}{8}$** | *Reversed:* A real number chosen at random from $[4, k]$ is less than $10$ with probability $\frac{3}{8}$. Find $k$. **KEY: $20$** |
| 3 | An elevator returns to the lobby every $40$ seconds; Zoe walks up at a random moment. Probability she waits less than $14$ seconds. **KEY: $\frac{7}{20}$** | A train leaves every $24$ minutes; Ravi arrives at a random moment. Probability he waits MORE than $14$ minutes. **KEY: $\frac{5}{12}$** | *Reversed:* A shuttle runs every $T$ minutes; a random arrival waits less than $4$ minutes with probability $\frac{2}{9}$. Find $T$. **KEY: $18$** |
| 4 | Points $A$, $C$, $B$ lie on a line in that order with $AC = 5$, $CB = 11$; $P$ random on $AB$. Probability $P$ is closer to $C$ than to $B$ (boundary: midpoint of $CB$ at $10.5$ from $A$). **KEY: $\frac{21}{32}$** | Segment $AB$ has length $20$ with a marked point $M$ at distance $8$ from $A$; $P$ random on $AB$. Probability $P$ is closer to $M$ than to $A$ (boundary at $4$). **KEY: $\frac{4}{5}$** | *Reversed:* Points $A$, $C$, $B$ in order with $AC = 4$, $CB = b$; a random point of $AB$ is closer to $C$ than to $B$ with probability $\frac{7}{10}$. Find $b$. **KEY: $6$** |
| 5 | A signal cycles green $35$ s, yellow $5$ s, red $32$ s; a car arrives at a random moment. Probability the light is NOT green. **KEY: $\frac{37}{72}$** | A washer cycles wash $22$ min, rinse $10$ min, spin $13$ min; Lin peeks at a random moment. Probability it is rinsing or spinning. **KEY: $\frac{23}{45}$** | *Reversed:* A light cycles red $r$ s, green $42$ s, yellow $6$ s; a random arrival sees red with probability $\frac{1}{4}$. Find $r$. **KEY: $16$** |
| 6 | $x$ random from $[-3, 7]$. Probability $x^3 < 64$ (i.e. $x < 4$). **KEY: $\frac{7}{10}$** | $x$ random from $[-5, 6]$. Probability $x^2 \ge 16$ (i.e. $x \le -4$ or $x \ge 4$; lengths $1 + 2$). **KEY: $\frac{3}{11}$** | *Derived:* $x$ random from $[-6, 10]$. Probability BOTH $x^2 < 25$ and $x^3 > -8$ (i.e. $-2 < x < 5$). **KEY: $\frac{7}{16}$** |
| 7 | A $30$-cm ribbon is cut at a random point. Probability the shorter piece is at most $6$ cm (cut in $[0,6] \cup [24,30]$). **KEY: $\frac{2}{5}$** | A $42$-inch cord is cut at a random point. Probability the longer piece is at least $7$ times the shorter (shorter $\le \frac{42}{8}$, cut within $5.25$ of an end). **KEY: $\frac{1}{4}$** | *Reversed:* A $40$-ft rope is cut at a random point; the shorter piece is at least $k$ ft with probability $\frac{3}{10}$. Find $k$ (cut in $[k, 40-k]$). **KEY: $14$** |
| 8 | $x$ random from $[0, 24]$. Probability $|x-6| < 4$ or $|x-13| < 5$ (union $(2,18)$, overlap on $(8,10)$). **KEY: $\frac{2}{3}$** | $x$ random from $[0, 15]$. Probability BOTH $|x-7| \le 5$ and $|x-10| \le 4$ (intersection $[6,12]$). **KEY: $\frac{2}{5}$** | *Derived:* $x$ random from $[0, 18]$. Probability $|x-9| > 5$ (far from the middle; two stretches of length $4$). **KEY: $\frac{4}{9}$** |
| 9 | $P$ random on segment $AB$ of length $14$. Probability $PA \cdot PB > 33$ (roots $3$ and $11$). **KEY: $\frac{4}{7}$** | $P$ random on segment $AB$ of length $12$. Probability $2 \cdot PA + PB < 16$ (linear: $PA < 4$). **KEY: $\frac{1}{3}$** | *Derived:* $P$ random on segment $AB$ of length $11$. Probability $PA \cdot PB \ge 24$ (roots $3$ and $8$). **KEY: $\frac{5}{11}$** |
| 10 | Route A leaves every $10$ min ($0, 10, 20, \ldots$ past), Route B every $15$ min starting $4$ past ($4, 19, 34, \ldots$); Kim takes whichever leaves first. Probability she waits less than $2$ min (period $30$: departures $0,4,10,19,20$; windows before $19$ and $20$ overlap, favorable $2+2+2+3 = 9$). **KEY: $\frac{3}{10}$** | A red lamp is lit for the first $3$ s of every $10$-s cycle; a blue lamp is lit for $4$ s starting at second $5$ of every $20$-s cycle. Probability at least one lamp is lit at a random moment (period $20$: red $[0,3) \cup [10,13)$, blue $[5,9)$, no overlap). **KEY: $\frac{1}{2}$** | *Reversed:* Two shuttle lines each run every $15$ min; line B leaves $k$ min after each line A departure ($0 < k < 3$). A random arrival boards whichever comes first and waits less than $3$ min with probability $\frac{1}{3}$ (windows overlap: favorable $3 + k$). Find $k$. **KEY: $2$** |

## §10.3 Probability Using Areas — ch10-s2.js
base indices: 2 0 3 1 0 2 1 3 1 0
v1 indices:   3 1 0 2 1 3 2 0 2 1
v2 indices:   1 3 2 0 3 1 0 2 0 3
v3 indices:   0 2 1 3 2 0 3 1 3 2

Every slot keeps a to-scale figure (total region + favorable sub-region as stated).

| slot | v1 | v2 | v3 |
|---|---|---|---|
| 1 | Triangle with base $9$ along the bottom of a $10 \times 8$ rectangle, apex on the top side. Probability inside the triangle ($\frac{36}{80}$). Figure: rectangle, triangle e.g. $(0.5,0)$–$(9.5,0)$–$(4,8)$. **KEY: $\frac{9}{20}$** | Trapezoid spanning the full height of a $10 \times 6$ rectangle, parallel sides $8$ (bottom) and $4$ (top). Probability inside the trapezoid ($\frac{36}{60}$). Figure: rectangle, trapezoid $(1,0)$–$(9,0)$–$(6.5,6)$–$(2.5,6)$. **KEY: $\frac{3}{5}$** | *Reversed:* Triangle with base $b$ along the bottom of a $14 \times 8$ rectangle, apex on the top side; probability inside is $\frac{2}{7}$. Find $b$ ($4b/112$). Figure: rectangle, triangle $(3,0)$–$(11,0)$–$(7,8)$. **KEY: $8$** |
| 2 | Quarter circle of radius $4$ centered at a corner of an $8 \times 4$ rectangle. Probability inside it ($\frac{4\pi}{32}$). Figure: rectangle, arc from corner. **KEY: $\frac{\pi}{8}$** | Semicircle of radius $3$ with diameter along the bottom side of a $9 \times 5$ rectangle. Probability inside it ($\frac{9\pi/2}{45}$). Figure: rectangle, semicircle centered at $(4.5, 0)$. **KEY: $\frac{\pi}{10}$** | *Reversed:* Quarter circle of radius $r$ centered at a corner of a square of side $10$; probability inside is $\frac{\pi}{8}$. Find $r$ ($\frac{\pi r^2/4}{100}$, $r^2 = 50$). Figure: square, arc radius $\approx 7.07$. **KEY: $5\sqrt{2}$** |
| 3 | Square of side $10$; probability the point is within $2$ of at least one side (inner square side $6$, border $\frac{64}{100}$). Figure: square with inner square, `ring`. **KEY: $\frac{16}{25}$** | $12 \times 8$ rectangle; probability within $1$ of at least one side (inner $10 \times 6$, border $\frac{36}{96}$). Figure: rectangle with inner rectangle, `ring`. **KEY: $\frac{3}{8}$** | *Reversed:* Square of side $9$; a random point is within $w$ of at least one side with probability $\frac{5}{9}$. Find $w$ ($(9-2w)^2 = 36$). Figure: square with inner square side $6$. **KEY: $\frac{3}{2}$** |
| 4 | $x, y$ each random from $[0, 5]$. Probability $x + y < 3$ (corner triangle legs $3$, $\frac{9/2}{25}$). Figure: $5 \times 5$ square, triangle. **KEY: $\frac{9}{50}$** | $x, y$ each random from $[0, 6]$. Probability both $x > 2$ AND $y > 2$ (a $4 \times 4$ sub-square). Figure: $6 \times 6$ square, sub-square. **KEY: $\frac{4}{9}$** | *Derived:* $x, y$ each random from $[0, 4]$. Probability $2 < x + y < 5$ (band: $16 - 2 - \frac{9}{2} = \frac{19}{2}$). Figure: $4 \times 4$ square, hexagonal band between the two lines. **KEY: $\frac{19}{32}$** |
| 5 | Circular mat radius $10$ with a concentric center disk radius $4$; probability on the disk ($\frac{16}{100}$). Figure: two concentric circles, dashed radii. **KEY: $\frac{4}{25}$** | Circular target radius $8$; probability the point lands OUTSIDE the concentric inner circle of radius $5$ ($\frac{64-25}{64}$). Figure: circle with inner circle, favorable ring via `ring`. **KEY: $\frac{39}{64}$** | *Reversed:* Bullseye radius $r$ concentric in a target of radius $12$; probability $\frac{1}{16}$. Find $r$ ($r^2/144$). Figure: target with bullseye radius $3$. **KEY: $3$** |
| 6 | Point random in a $14 \times 6$ rectangle; probability it is closer to the bottom side than to each of the other three sides ($y < 3$, $y < x$, $y < 14 - x$: trapezoid $(0,0)$–$(14,0)$–$(11,3)$–$(3,3)$, area $33$). Figure: rectangle, trapezoid, dashed corner bisectors. **KEY: $\frac{11}{28}$** | A $9 \times 6$ park has snack stands at $P(2,3)$ and $Q(8,3)$; a picnicker sits at a random point. Probability closer to $P$ than to $Q$ (bisector $x = 5$). Figure: rectangle, two points, dashed bisector. **KEY: $\frac{5}{9}$** | *Reversed:* Rectangle $w \times 4$; a random point is closer to the bottom side than to each of the other three with probability $\frac{3}{10}$ (trapezoid area $\frac{(w + (w-4)) \cdot 2}{2} = 2w - 4$ over $4w$). Find $w$. Figure: $5 \times 4$ rectangle, trapezoid $(0,0)$–$(5,0)$–$(3,2)$–$(2,2)$. **KEY: $5$** |
| 7 | Semicircle of radius $3$ with diameter along the bottom side of an $8 \times 3$ rectangle (bulge exactly reaches the top). Probability inside ($\frac{9\pi/2}{24}$). Figure: rectangle, semicircle centered $(4,0)$. **KEY: $\frac{3\pi}{16}$** | Right triangle with legs $6$ and $8$ and its inscribed circle (radius $\frac{6+8-10}{2} = 2$, center $(2,2)$). Probability inside the circle ($\frac{4\pi}{24}$). Figure: triangle, incircle via `circlePts`. **KEY: $\frac{\pi}{6}$** | *Reversed:* Semicircle whose diameter is the ENTIRE bottom side of a $w \times 5$ rectangle; probability inside is $\frac{\pi}{4}$ ($\frac{\pi w}{40}$, radius $\frac{w}{2} \le 5$). Find $w$. Figure: $10 \times 5$ rectangle, semicircle radius $5$. **KEY: $10$** |
| 8 | $x, y$ each random from $[0, 8]$. Probability $|x - y| \ge 3$ (two corner triangles legs $5$, $\frac{25}{64}$). Figure: $8 \times 8$ square, two triangles. **KEY: $\frac{25}{64}$** | A taxi reaches a corner at a random time $x$ in a $10$-minute span; its passenger reaches it at a random time $y$ in the FIRST $5$ minutes of that span; they connect if $|x - y| \le 2$. Probability ($\frac{18}{50}$, clipped band in a $10 \times 5$ rectangle). Figure: rectangle, band between $y = x \pm 2$. **KEY: $\frac{9}{25}$** | *Derived:* $x, y$ each random from $[0, 6]$. Probability $|x - y| \ge 2$ AND both numbers $\ge 2$ (two triangles legs $2$ inside $[2,6]^2$, $\frac{4}{36}$). Figure: square, sub-square, two small triangles. **KEY: $\frac{1}{9}$** |
| 9 | Cara and Dan each arrive at random in a $60$-min window; Cara waits $15$ min, Dan waits $25$. Probability they meet (fail triangles legs $45$ and $35$: $3600 - \frac{2025+1225}{2} = 1975$). Figure: $60 \times 60$ square, meeting band. **KEY: $\frac{79}{144}$** | Eli arrives at random in a $40$-min span, Fay in the first $20$ min of it; they meet if within $5$ min of each other ($\frac{187.5}{800}$, clipped band in $40 \times 20$). Figure: rectangle, band. **KEY: $\frac{15}{64}$** | *Reversed:* Two friends each arrive at random in a $60$-min window and each waits $w$ min; they meet with probability $\frac{7}{16}$ ($1 - (\frac{60-w}{60})^2$). Find $w$. Figure: square, band for $w = 15$. **KEY: $15$** |
| 10 | $x, y$ each random from $[0, 8]$. Probability $x$, $y$, and $5$ can be the sides of a triangle (fails: $x+y<5$ triangle $\frac{25}{2}$, $|x-y|>5$ two triangles $\frac{9}{2}$ each; $\frac{42.5}{64}$). Figure: square, pentagon favorable region. **KEY: $\frac{85}{128}$** | Two rods of random lengths $x, y$, each uniform on $[0, 4]$, plus a rod of length $5$. Probability they form a triangle (only $x + y > 5$ can fail: favorable corner triangle legs $3$, $\frac{9/2}{16}$). Figure: $4 \times 4$ square, corner triangle at $(4,4)$. **KEY: $\frac{9}{32}$** | *Derived:* $x, y$ each random from $[0, 6]$. Probability $x$, $y$, $3$ form a triangle with perimeter LESS than $9$ (triangle conditions plus $x+y<6$: strip $3 < x+y < 6$ minus two side triangles $= 9$). Figure: square, quadrilateral band region. **KEY: $\frac{1}{4}$** |

## Challenge — ch10-cw.js
base indices: 0 2 1 3 2 0 1 3 0 2 1 3
v1 indices:   1 3 2 0 3 1 2 0 1 3 2 0
v2 indices:   3 1 0 2 1 3 0 2 3 1 0 2
v3 indices:   2 0 3 1 0 2 3 1 2 0 3 1

| slot | v1 | v2 | v3 |
|---|---|---|---|
| 1 | Point random on a segment of length $18$; probability within $2$ of the midpoint (stretch of length $4$). **KEY: $\frac{2}{9}$** | A $40$-minute recital; Ken slips in at a random moment. Probability the moment is within $6$ minutes of the halfway point (minutes $14$ to $26$). **KEY: $\frac{3}{10}$** | *Reversed:* On a segment of length $30$, a random point is within $d$ of the midpoint with probability $\frac{7}{15}$. Find $d$ ($\frac{2d}{30}$). **KEY: $7$** |
| 2 | $x$ random from $[2, 17]$. Probability $|x - 12| < 4$ (stretch $(8, 16)$, length $8$ of $15$). **KEY: $\frac{8}{15}$** | $x$ random from $[0, 16]$. Probability $x$ is closer to $4$ than to $10$, i.e. $|x-4| < |x-10|$ (boundary $7$). **KEY: $\frac{7}{16}$** | *Reversed:* $x$ random from $[0, 24]$ satisfies $|x - 9| \le r$ with probability $\frac{3}{4}$. Find $r$ (favorable $[0, 18]$ — the left end clamps at $0$ exactly). **KEY: $9$** |
| 3 | AREA. Square of side $6$ centered in a circle of radius $5$ (fits: half-diagonal $3\sqrt{2} < 5$). Probability inside the square ($\frac{36}{25\pi}$). Figure: circle, centered square, dashed radius. **KEY: $\frac{36}{25\pi}$** | AREA. An $8 \times 4$ rectangle centered in a circle of radius $5$ (half-diagonal $\sqrt{20} < 5$). Probability inside the rectangle ($\frac{32}{25\pi}$). Figure: circle, centered rectangle. **KEY: $\frac{32}{25\pi}$** | *Reversed:* A square of side $s$ centered in a circle of radius $10$ is hit with probability $\frac{121}{100\pi}$. Find $s$ ($\frac{s^2}{100\pi}$; half-diagonal $\frac{11\sqrt{2}}{2} < 10$ so it fits). Figure: circle radius $10$, centered square side $11$. **KEY: $11$** |
| 4 | A sign shows GO for $18$ s then WAIT for $30$ s, repeating; Tess arrives at a random moment and can start within $12$ s if it is GO or in the last $12$ s of WAIT ($\frac{18+12}{48}$). **KEY: $\frac{5}{8}$** | A fountain sprays for $40$ s then rests for $50$ s, repeating; Vic photographs the spray and will wait at most $20$ s ($\frac{40+20}{90}$). **KEY: $\frac{2}{3}$** | *Reversed:* A signal shows WALK for $w$ s then DON’T WALK for $48$ s; a random arrival can start within $12$ s with probability $\frac{1}{2}$ ($\frac{w+12}{w+48}$). Find $w$. **KEY: $24$** |
| 5 | AREA. Point random in a disk of radius $8$; probability CLOSER TO THE CENTER than to the edge ($d < 4$, $\frac{16}{64}$). Figure: circle, inner circle radius $4$. **KEY: $\frac{1}{4}$** | AREA. Point random in a disk of radius $10$; probability its distance from the center is between $4$ and $7$ ($\frac{49-16}{100}$). Figure: circle with circles radius $4$ and $7$, favorable ring. **KEY: $\frac{33}{100}$** | *Derived:* Point random in a disk of radius $12$; probability it is closer to the edge than to the center but NOT within $3$ of the edge ($6 < d < 9$, $\frac{81-36}{144}$). Figure: circle, ring between radii $6$ and $9$. **KEY: $\frac{5}{16}$** |
| 6 | Square $ABCD$ side $12$, $P$ random on $\overline{AB}$; probability area of $\triangle PBC < 30$ ($6 \cdot PB < 30$, $PB < 5$). Figure: square, sample segment $PC$. **KEY: $\frac{5}{12}$** | Rectangle $ABCD$ with $AB = 10$, $BC = 4$; $P$ random on $\overline{AB}$; probability area of $\triangle PBC > 6$ ($2 \cdot PB > 6$). Figure: rectangle, segment $PC$. **KEY: $\frac{7}{10}$** | *Reversed:* Square $ABCD$ side $s$, $P$ random on $\overline{AB}$; area of $\triangle PBC < 21$ with probability $\frac{7}{24}$ ($PB < \frac{42}{s}$, so $\frac{42}{s^2} = \frac{7}{24}$). Find $s$. Figure: square side $12$. **KEY: $12$** |
| 7 | AREA. Four quarter circles of radius $2$ at the corners of a square of side $6$ (combined area $4\pi$, $\frac{4\pi}{36}$). Figure: square, four corner arcs. **KEY: $\frac{\pi}{9}$** | AREA. Two semicircles of radius $3$ whose diameters are the two SHORT sides (length $6$) of a $10 \times 6$ rectangle, bulging inward (combined $9\pi$, no overlap since $3+3 < 10$; $\frac{9\pi}{60}$). Figure: rectangle, two semicircles. **KEY: $\frac{3\pi}{20}$** | *Reversed:* Four quarter circles of radius $r$ at the corners of a square of side $12$ are hit with probability $\frac{\pi}{9}$ ($\frac{\pi r^2}{144}$). Find $r$. Figure: square side $12$, corner arcs radius $4$. **KEY: $4$** |
| 8 | AREA. Gia arrives at random in a $50$-min window, Hal in the first $25$ min of it; they meet if within $5$ min ($\frac{237.5}{1250}$, clipped band in $50 \times 25$). Figure: rectangle, band. **KEY: $\frac{19}{100}$** | AREA. Ida is free 1:00–1:30, Jon is free 1:15–1:45, each arriving at a random moment of their own window; they meet if within $8$ min ($\frac{240}{900}$ on the shifted $30 \times 30$ square). Figure: square with offset axis labels, band. **KEY: $\frac{4}{15}$** | *Derived:* In the base setup (Cleo $[0,60]$, Dev $[0,30]$, meet if within $10$), GIVEN that they meet, probability Dev arrived first ($\frac{300}{550}$: the $0 \le x - y \le 10$ strip has area $300$ of the meeting region’s $550$). Figure: $60 \times 30$ rectangle, band split by $y = x$. **KEY: $\frac{6}{11}$** |
| 9 | AREA. $x, y$ each random from $[0, 8]$; probability $|x-4| + |y-4| < 3$ (tilted square, diagonals $6$: area $18$ of $64$). Figure: square, diamond centered $(4,4)$. **KEY: $\frac{9}{32}$** | AREA. $x, y$ each random from $[0, 10]$; probability BOTH $|x-5| < 2$ and $|y-5| < 2$ (upright square side $4$, $\frac{16}{100}$). Figure: square, centered sub-square. **KEY: $\frac{4}{25}$** | *Derived:* $x, y$ each random from $[0, 4]$; probability $1 < |x-2| + |y-2| < 2$ (diamond ring: $2 \cdot 2^2 - 2 \cdot 1^2 = 6$ of $16$). Figure: square, two nested diamonds via `ring`. **KEY: $\frac{3}{8}$** |
| 10 | An $18$-inch rope is cut at two random points; probability EVERY piece is longer than $4$ ($\frac{(18-12)^2}{18^2}$, both orders). Figure: $18 \times 18$ square, two small triangles. **KEY: $\frac{1}{9}$** | A $16$-inch stick is cut at two random points; probability the MIDDLE piece is longer than $4$ ($|x-y| > 4$: two triangles legs $12$, $\frac{144}{256}$). Figure: $16 \times 16$ square, two corner triangles. **KEY: $\frac{9}{16}$** | *Derived:* A stick of length $8$ is cut at THREE random points; probability the four pieces can form a quadrilateral (each piece $< 4$: $1 - 4 \cdot (\frac{1}{2})^3$). No figure (3D region); solution argues by the largest piece. **KEY: $\frac{1}{2}$** |
| 11 | Someone glances at a wall clock at a random moment of a $12$-hour period; probability the smaller angle between the hands is less than $24°$ ($\frac{48}{360}$). **KEY: $\frac{2}{15}$** | Two fair spinner arrows each stop pointing in a random direction; probability the angle between them is less than $40°$ (relative direction uniform, $\frac{80}{360}$). **KEY: $\frac{2}{9}$** | *Derived:* Same clock glance; probability the smaller angle between the hands is GREATER than $120°$ (relative angle in $(120°, 240°)$, $\frac{120}{360}$). **KEY: $\frac{1}{3}$** |
| 12 | AREA. $x, y$ each random from $[0, 6]$; probability each is less than THREE times the other and $x + y > 4$ (wedge $36 - 2 \cdot 6 = 24$ minus triangle $(0,0)$–$(3,1)$–$(1,3)$ of area $4$; $\frac{20}{36}$). Figure: square, dashed $y = 3x$, $y = \frac{x}{3}$, $x+y=4$, favorable pentagon. **KEY: $\frac{5}{9}$** | AREA. $x, y$ each random from $[0, 6]$; probability $y > x$ AND $x + 2y < 8$ (region area $\frac{16}{3}$ of $36$). Figure: square, dashed $y = x$ and $x + 2y = 8$, favorable triangle $(0,0)$–$(\frac{8}{3},\frac{8}{3})$–$(0,4)$. **KEY: $\frac{4}{27}$** | *Derived:* $x, y$ each random from $[0, 6]$; probability each is less than twice the other AND $x + y < 6$ (wedge below the sum line: triangle $(0,0)$–$(4,2)$–$(2,4)$, area $6$ of $36$). Figure: square, dashed $y=2x$, $y=\frac{x}{2}$, $x+y=6$, favorable triangle. **KEY: $\frac{1}{6}$** |

## Worksheet — ch10-ws.js (free response; keys are LaTeX strings, no indices)

| slot | v1 | v2 | v3 |
|---|---|---|---|
| 1 | Points $D$, $E$, $F$ on a line in that order, $DE = 4$, $EF = 7$; random point on $\overline{DF}$. Probability it lies on $\overline{EF}$. **KEY: $\frac{7}{11}$** | A $24$-m fence has a painted stretch from the $5$-m mark to the $16$-m mark; a sparrow lands at a random point. Probability it lands on paint. **KEY: $\frac{11}{24}$** | *Reversed:* Points $G$, $H$, $K$ on a line in that order with $GK = 35$; a random point of $\overline{GK}$ lies on $\overline{GH}$ with probability $\frac{3}{5}$. Find $GH$. **KEY: $21$** |
| 2 | A gondola departs every $18$ minutes; Noor arrives at a random moment. Probability she waits between $4$ and $10$ minutes. **KEY: $\frac{1}{3}$** | A tram departs every $24$ minutes; probability a random arrival waits LESS than $5$ minutes OR MORE than $16$ ($\frac{5+8}{24}$). **KEY: $\frac{13}{24}$** | *Reversed:* A boat departs every $T$ minutes; a random arrival waits between $5$ and $12$ minutes with probability $\frac{7}{22}$. Find $T$. **KEY: $22$** |
| 3 | $x$ random from $[0, 50]$. Probability $\sqrt{x} < 6$ ($x < 36$). **KEY: $\frac{18}{25}$** | $x$ random from $[0, 22]$. Probability $\sqrt{x + 3} < 4$ ($x < 13$). **KEY: $\frac{13}{22}$** | *Reversed:* $x$ random from $[0, m]$ satisfies $\sqrt{x} < 4$ with probability $\frac{8}{13}$. Find $m$ ($\frac{16}{m}$). **KEY: $26$** |
| 4 | AREA. Three non-overlapping circles of radius $1$ painted in a square of side $6$ (combined $3\pi$ of $36$). Figure: square, circles at e.g. $(1.5,1.5)$, $(4.5,1.5)$, $(3,4.5)$. **KEY: $\frac{\pi}{12}$** | AREA. Two non-overlapping $2 \times 3$ rectangles painted inside a circle of radius $4$ (combined $12$ of $16\pi$). Figure: circle, rectangles at $x \in [-3,-1]$ and $[1,3]$, $|y| \le 1.5$ (corners at distance $\sqrt{11.25} < 4$). **KEY: $\frac{3}{4\pi}$** | *Reversed:* Two non-overlapping circles of radius $2$ painted in an $8 \times w$ rectangle; a random point is on paint with probability $\frac{\pi}{13}$ ($\frac{8\pi}{8w}$). Find $w$. Figure: $8 \times 13$, circles at $(2,2)$ and $(6,11)$. **KEY: $13$** |
| 5 | AREA. A $10 \times 6$ pool with a $3$-m deck on ALL four sides (outer $16 \times 12$); probability a leaf on the outer rectangle lands on deck ($\frac{192-60}{192}$). Figure: outer rectangle, inner pool centered. **KEY: $\frac{11}{16}$** | AREA. A $12 \times 10$ patio with a $4 \times 5$ stage in one corner; probability a random point of the patio is NOT on the stage ($\frac{100}{120}$). Figure: rectangle, corner rectangle. **KEY: $\frac{5}{6}$** | *Reversed:* A $12 \times 10$ pool has decks of width $d$ along BOTH long ($12$-m) sides; a point of the full $12 \times (10+2d)$ rectangle is on deck with probability $\frac{1}{3}$ ($\frac{24d}{12(10+2d)}$). Find $d$. Figure: $12 \times 15$ with two $12 \times 2.5$ strips. **KEY: $\frac{5}{2}$** |
| 6 | AREA. $x, y$ each random from $[0, 8]$; probability $y > 3x$ (triangle $(0,0)$–$(0,8)$–$(\frac{8}{3},8)$, area $\frac{32}{3}$ of $64$). Figure: square, line, triangle. **KEY: $\frac{1}{6}$** | AREA. $x, y$ each random from $[0, 10]$; probability $y < \frac{x}{2} + 1$ (trapezoid under the line from $(0,1)$ to $(10,6)$, area $35$ of $100$). Figure: square, line, trapezoid. **KEY: $\frac{7}{20}$** | *Reversed:* $x$ random from $[0, a]$ with $a \le 5$, $y$ random from $[0, 10]$; $y > 2x$ with probability $\frac{13}{20}$ ($1 - \frac{a}{10}$, the line exits the top). Find $a$. Figure: $3.5 \times 10$ rectangle, line $y = 2x$. **KEY: $\frac{7}{2}$** |
| 7 | AREA. Kai and Lee each arrive at random in a $40$-min window; probability Kai is first AND Lee comes more than $15$ min later (triangle legs $25$, $\frac{312.5}{1600}$). Figure: $40 \times 40$ square, corner triangle. **KEY: $\frac{25}{128}$** | AREA. Pia and Quinn each arrive at random in a $60$-min window; probability Pia is first AND they arrive within $20$ min of each other (half-band $0 < y - x < 20$, area $1000$ of $3600$). Figure: $60 \times 60$ square, half-band above the diagonal. **KEY: $\frac{5}{18}$** | *Reversed:* Rae and Sid each arrive at random in a $36$-min window; Rae is first and Sid comes more than $w$ min later with probability $\frac{2}{81}$ ($\frac{(36-w)^2/2}{1296}$). Find $w$. Figure: square, small corner triangle legs $8$. **KEY: $28$** |
| 8 | AREA. A regular hexagon inscribed in a circle of radius $4$ (vertices on the circle); probability inside the hexagon ($\frac{24\sqrt{3}}{16\pi}$). Figure: circle, hexagon. **KEY: $\frac{3\sqrt{3}}{2\pi}$** | AREA. An equilateral triangle inscribed in a circle of radius $6$; probability inside the triangle ($\frac{27\sqrt{3}}{36\pi}$). Figure: circle, triangle. **KEY: $\frac{3\sqrt{3}}{4\pi}$** | *Derived:* A square sits in a semicircle of radius $5$ with its base on the diameter, centered, top corners on the arc ($s^2 + (\frac{s}{2})^2 = 25$, so $s^2 = 20$); probability a random point of the semicircle is inside the square ($\frac{20}{25\pi/2}$). Figure: semicircle, square half-width $\sqrt{5}$, height $2\sqrt{5}$. **KEY: $\frac{8}{5\pi}$** |
| 9 | $x$ random from $[0, 14]$. Probability within $1$ of a multiple of $4$ ($[0,1] \cup [3,5] \cup [7,9] \cup [11,13]$, length $7$). **KEY: $\frac{1}{2}$** | A $22$-cm strip has staples at $0$, $10$, and $20$ cm; a random point is within $2$ cm of a staple ($[0,2] \cup [8,12] \cup [18,22]$, length $10$). **KEY: $\frac{5}{11}$** | *Derived:* $x$ random from $[0, 30]$. Probability its distance to the NEAREST multiple of $7$ is more than $2$ (complement of $[0,2] \cup [5,9] \cup [12,16] \cup [19,23] \cup [26,30]$, favorable $12$). **KEY: $\frac{2}{5}$** |
| 10 | AREA. $x, y$ each random from $[0, 8]$; probability $5 < x + y < 11$ (band: $64$ minus two corner triangles legs $5$; $\frac{39}{64}$). Figure: square, band. **KEY: $\frac{39}{64}$** | AREA. $x$ random from $[0, 10]$, $y$ random from $[0, 4]$; probability $x + y < 3$ (corner triangle legs $3$, $\frac{9/2}{40}$). Figure: $10 \times 4$ rectangle, triangle. **KEY: $\frac{9}{80}$** | *Derived:* $x, y$ each random from $[0, 6]$; probability $4 < x + y < 8$ AND $x < y$ (the band is symmetric across $y = x$, so half of $\frac{20}{36}$). Figure: square, band, dashed diagonal, favorable half. **KEY: $\frac{5}{18}$** |

## Verification record
All $126$ keys computed exactly in node (closed-form region measure over total) and each
cross-checked by Monte Carlo of the exact stated condition ($2$M samples, $0$ outside $4\sigma$;
scratch `cp10_verify.mjs`). Round audit (`cp10_keys.mjs`): v1, v2, v3 each $42$ distinct
normalized keys, $0$ repeated; $0$ slots with a repeated key across base|v1|v2|v3.
